def solution(n):
    lst = [4,1,2]
    rlt = ''
    while n!=0:
        rlt += str(lst[n % 3])
        if n % 3==0:
            n = n//3 - 1
        else:
            n = n//3

    rlt = rlt[::-1]
    return rlt